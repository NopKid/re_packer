/* unpacker.c — read packed binary, locate metadata, XOR-decrypt and dump original .text to file for verification */
#include <stdio.h>
#include <stdint.h>
int main(int argc, char **argv) { /* ... */ }
